import React,{useCallback,useEffect,useState}from'react';import{ActivityIndicator,RefreshControl,ScrollView,StyleSheet,Text,View}from'react-native';import{Ionicons}from'@expo/vector-icons';import{useSafeAreaInsets}from'react-native-safe-area-context';import{PageCarousel}from'../components/PageCarousel';import{taurantoApi}from'../lib/api';import{colors,darkColors}from'../theme/tokens';import{useTheme}from'../theme/ThemeContext';
const VIEWS=['Overview','Stock','Review Queue','Suppliers','Usage','Activity']as const;type ViewKey=typeof VIEWS[number];
export function InventoryScreen({restaurantId}:{restaurantId:string}){const i=useSafeAreaInsets(),{dark}=useTheme(),[view,setView]=useState<ViewKey>('Overview'),[data,setData]=useState<any>(null),[busy,setBusy]=useState(true),[error,setError]=useState('');const load=useCallback(async()=>{setBusy(true);setError('');try{setData(await taurantoApi.inventory(restaurantId))}catch(e){setError(e instanceof Error?e.message:'Inventory could not load.')}finally{setBusy(false)}},[restaurantId]);useEffect(()=>{void load()},[load]);const items=data?.items||[],low=items.filter((x:any)=>Number(x.quantity)<=Number(x.reorder_level));return <ScrollView style={[s.page,dark&&s.pageDark]} contentContainerStyle={{paddingTop:i.top+66,paddingHorizontal:18,paddingBottom:i.bottom+115}} refreshControl={<RefreshControl refreshing={busy} onRefresh={load}/>}><Text style={[s.title,dark&&s.textDark]}>Inventory</Text><Text style={s.copy}>Stock, suppliers, review decisions and usage share one restaurant record.</Text><PageCarousel items={VIEWS} active={view} onChange={setView}/>{busy&&!data?<ActivityIndicator style={{marginTop:40}} color={colors.leafDeep}/>:error?<State title="Inventory unavailable" copy={error}/>:view==='Overview'?<><View style={s.metrics}><Metric label="ITEMS" value={data?.metrics?.items||0}/><Metric label="LOW" value={data?.metrics?.lowStock||0}/><Metric label="OUT" value={data?.metrics?.outOfStock||0}/></View><Text style={[s.heading,dark&&s.textDark]}>Needs attention</Text>{low.length?low.map((x:any)=><Item key={x.id} x={x} dark={dark}/>):<State title="Stock looks healthy" copy="Items at or below their reorder level appear here."/>}</>:view==='Stock'?<Rows rows={items} dark={dark}/>:view==='Review Queue'?<Rows rows={low} dark={dark} empty="No inventory reviews waiting"/>:view==='Suppliers'?<Suppliers rows={data?.suppliers||[]} dark={dark}/>:view==='Usage'?<Usage rows={data?.events||[]} dark={dark}/>:<Activity rows={data?.events||[]} dark={dark}/>}</ScrollView>}
function Metric({label,value}:any){return <View style={s.metric}><Text style={s.metricLabel}>{label}</Text><Text style={s.metricValue}>{value}</Text></View>}function Item({x,dark}:any){const low=Number(x.quantity)<=Number(x.reorder_level);return <View style={[s.card,dark&&s.cardDark]}><View style={[s.icon,{backgroundColor:low?'#FFF0EC':'#EAF7F0'}]}><Ionicons name="cube-outline" size={21} color={low?'#B85045':colors.leafDeep}/></View><View style={{flex:1}}><Text style={[s.cardTitle,dark&&s.textDark]}>{x.name}</Text><Text style={s.meta}>{x.category} · {x.quantity} {x.unit} · reorder at {x.reorder_level}</Text></View></View>}
function Rows({rows,dark,empty='No stock items yet'}:any){return <>{rows.length?rows.map((x:any)=><Item key={x.id} x={x} dark={dark}/>):<State title={empty} copy="Database-backed inventory records will appear here."/>}</>}function Suppliers({rows,dark}:any){return <>{rows.length?rows.map((x:any)=><View key={x.id} style={[s.card,dark&&s.cardDark]}><Ionicons name="business-outline" size={22} color="#756889"/><View style={{flex:1}}><Text style={[s.cardTitle,dark&&s.textDark]}>{x.name}</Text><Text style={s.meta}>{[x.category,x.contact_name,x.phone].filter(Boolean).join(' · ')}</Text></View></View>):<State title="No suppliers connected" copy="Suppliers added to the operations directory will appear here."/>}</>}function Usage({rows,dark}:any){const usage=rows.filter((x:any)=>Number(x.quantity_delta)<0);return <Activity rows={usage} dark={dark} empty="No usage recorded yet"/>}function Activity({rows,dark,empty='No inventory activity yet'}:any){return <>{rows.length?rows.map((x:any)=><View key={x.id} style={[s.card,dark&&s.cardDark]}><Ionicons name="pulse-outline" size={21} color={colors.leafDeep}/><View style={{flex:1}}><Text style={[s.cardTitle,dark&&s.textDark]}>{x.inventory_items?.name||'Inventory'}</Text><Text style={s.meta}>{String(x.event_type).replace(/_/g,' ')}{x.quantity_delta?` · ${x.quantity_delta}`:''} · {new Date(x.created_at).toLocaleDateString()}</Text></View></View>):<State title={empty} copy="Adjustments and stock events are retained here for audit."/>}</>}function State({title,copy}:any){const{dark}=useTheme();return <View style={[s.state,dark&&s.cardDark]}><Text style={[s.cardTitle,dark&&s.textDark]}>{title}</Text><Text style={s.meta}>{copy}</Text></View>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:'#FAFBF9'},pageDark:{backgroundColor:darkColors.bg},title:{fontFamily:'NunitoSans_900Black',fontSize:28,color:'#182019'},textDark:{color:darkColors.text},copy:{fontFamily:'NunitoSans_600SemiBold',fontSize:12,lineHeight:18,color:'#747D75',marginTop:4,marginBottom:18},metrics:{flexDirection:'row',gap:9,marginBottom:22},metric:{flex:1,borderWidth:1,borderColor:'#E3E7E3',borderRadius:17,padding:14,backgroundColor:'#fff'},metricLabel:{fontFamily:'NunitoSans_900Black',fontSize:9,color:'#7B837D'},metricValue:{fontFamily:'NunitoSans_900Black',fontSize:26,color:'#172019',marginTop:7},heading:{fontFamily:'NunitoSans_900Black',fontSize:15,color:'#172019',marginBottom:10},card:{minHeight:72,borderWidth:1,borderColor:'#E3E7E3',borderRadius:17,padding:13,backgroundColor:'#fff',flexDirection:'row',alignItems:'center',gap:11,marginBottom:9},cardDark:{backgroundColor:darkColors.card,borderColor:darkColors.border},icon:{width:42,height:42,borderRadius:13,alignItems:'center',justifyContent:'center'},cardTitle:{fontFamily:'NunitoSans_900Black',fontSize:14,color:'#202721'},meta:{fontFamily:'NunitoSans_600SemiBold',fontSize:10.5,lineHeight:16,color:'#7A827C',marginTop:3},state:{minHeight:150,borderWidth:1,borderColor:'#E3E7E3',borderRadius:20,backgroundColor:'#fff',padding:25,alignItems:'center',justifyContent:'center'}});