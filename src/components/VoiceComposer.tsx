import React,{useEffect,useRef,useState} from "react";
import {Alert,Platform,Pressable,StyleSheet,Switch,Text,TextInput,View} from "react-native";
import {Audio} from "expo-av";import * as Speech from "expo-speech";import {Ionicons} from "@expo/vector-icons";import {ExpoSpeechRecognitionModule,useSpeechRecognitionEvent} from "expo-speech-recognition";import {colors,radius,shadow} from "../theme/tokens";import {VoiceCommand} from "../lib/models";import {taurantoApi} from "../lib/api";
type Mode="off"|"standby"|"recording"|"working";
export type VoiceStatus={mode:Mode;message:string;lastHeard:string};
const wakeAliases=["hey tauranto","hi tauranto","hello tauranto","okay tauranto","ok tauranto","hey toronto","hi toronto","hello toronto"];
const normalize=(v:string)=>v.toLowerCase().replace(/[^a-z0-9\s]/g," ").replace(/\s+/g," ").trim();
const wakeMatch=(v:string)=>wakeAliases.find(a=>normalize(v).includes(a));
const commandAfterWake=(v:string)=>{const n=normalize(v),alias=wakeAliases.find(a=>n.includes(a));if(!alias)return"";return n.slice(n.indexOf(alias)+alias.length).replace(/^(please|can you|could you|would you)\s+/i,"").trim()};
const draft=(transcript:string):VoiceCommand=>({id:`capture-${Date.now()}`,transcript,title:"Restaurant instruction",summary:transcript,type:"task",status:"pending",createdAt:new Date().toISOString(),targets:[],confidence:1});
export function VoiceComposer({onCommand,enabled,onEnabledChange,onStatusChange,visible=true}:{onCommand:(command:VoiceCommand)=>void|Promise<void>;enabled:boolean;onEnabledChange:(enabled:boolean)=>void;onStatusChange?:(status:VoiceStatus)=>void;visible?:boolean}){const [mode,setMode]=useState<Mode>("off"),[message,setMessage]=useState(""),[transcript,setTranscript]=useState(""),[manual,setManual]=useState(false),[lastHeard,setLastHeard]=useState("");const modeRef=useRef<Mode>("off"),enabledRef=useRef(false),recordingRef=useRef<any>(null),finishingRef=useRef(false),heardSpeechRef=useRef(false),lastVoiceRef=useRef(0),restartRef=useRef<ReturnType<typeof setTimeout>|null>(null),wakeStartingRef=useRef(false),wakeTriggeredRef=useRef(false);const changeMode=(next:Mode)=>{modeRef.current=next;setMode(next)};
useSpeechRecognitionEvent("result",event=>{const heard=event.results[0]?.transcript?.trim()||"";if(!heard)return;setLastHeard(heard);if(modeRef.current==="standby"&&wakeMatch(heard)&&!wakeTriggeredRef.current){const trailing=commandAfterWake(heard);wakeTriggeredRef.current=true;ExpoSpeechRecognitionModule.stop();if(trailing.length>5&&event.isFinal){setTranscript(trailing);void submit(trailing);return}setMessage("Listening…");void startRecording()}});
useSpeechRecognitionEvent("end",()=>{if(enabledRef.current&&modeRef.current==="standby"&&!wakeTriggeredRef.current){if(restartRef.current)clearTimeout(restartRef.current);restartRef.current=setTimeout(()=>void startWakeSession(),250)}});useSpeechRecognitionEvent("error",event=>{if(!["aborted","no-speech"].includes(event.error))setMessage(`Voice service: ${event.message||event.error}`)});
useEffect(()=>{if(enabled!==enabledRef.current)void toggleStandby(enabled)},[enabled]);useEffect(()=>{onStatusChange?.({mode,message,lastHeard})},[mode,message,lastHeard,onStatusChange]);useEffect(()=>()=>{enabledRef.current=false;if(restartRef.current)clearTimeout(restartRef.current);ExpoSpeechRecognitionModule.abort();Speech.stop();if(recordingRef.current)void recordingRef.current.stopAndUnloadAsync()},[]);
async function permission(){const speech=await ExpoSpeechRecognitionModule.requestPermissionsAsync(),audio=await Audio.requestPermissionsAsync();if(!speech.granted||!audio.granted){Alert.alert("Microphone permission needed","Allow microphone and speech recognition access in device Settings.");return false}return true}
async function startWakeSession(){if(!enabledRef.current||wakeStartingRef.current||["recording","working"].includes(modeRef.current))return;wakeStartingRef.current=true;try{if(!(await permission()))return;wakeTriggeredRef.current=false;changeMode("standby");setMessage(Platform.OS==="web"?"Keep Tauranto open for standby.":"Listening for “Hey Tauranto”…");ExpoSpeechRecognitionModule.abort();setTimeout(()=>{if(enabledRef.current&&modeRef.current==="standby")try{ExpoSpeechRecognitionModule.start({lang:"en-US",interimResults:true,continuous:true,requiresOnDeviceRecognition:false,addsPunctuation:false})}catch(e){setMessage(e instanceof Error?e.message:"Could not start wake listening.")}},80)}finally{wakeStartingRef.current=false}}
async function toggleStandby(on:boolean){enabledRef.current=on;setLastHeard("");setMessage("");if(restartRef.current)clearTimeout(restartRef.current);ExpoSpeechRecognitionModule.abort();Speech.stop();if(!on){if(recordingRef.current)await finishRecording(false);changeMode("off");return}if(!(await permission())){enabledRef.current=false;onEnabledChange(false);changeMode("off");return}changeMode("standby");setTimeout(()=>void startWakeSession(),80)}
async function startRecording(){if(!(await permission()))return;ExpoSpeechRecognitionModule.abort();Speech.stop();finishingRef.current=false;heardSpeechRef.current=false;lastVoiceRef.current=Date.now();setTranscript("");setMessage("Listening… speak naturally, then stop.");changeMode("recording");try{await Audio.setAudioModeAsync({allowsRecordingIOS:true,playsInSilentModeIOS:true,shouldDuckAndroid:true});const recorder=new Audio.Recording();recordingRef.current=recorder;await recorder.prepareToRecordAsync({...Audio.RecordingOptionsPresets.HIGH_QUALITY,isMeteringEnabled:true} as any);recorder.setProgressUpdateInterval(100);recorder.setOnRecordingStatusUpdate((status:any)=>{if(!status.isRecording||finishingRef.current)return;const meter=typeof status.metering==="number"?status.metering:-160,now=Date.now();if(meter>-64){heardSpeechRef.current=true;lastVoiceRef.current=now}if((heardSpeechRef.current&&status.durationMillis>700&&now-lastVoiceRef.current>900)||status.durationMillis>30000)void finishRecording(true)});await recorder.startAsync()}catch(e){recordingRef.current=null;setMessage(e instanceof Error?e.message:"Could not begin command recording.");resume()}}
async function finishRecording(transcribe=true){const recorder=recordingRef.current;if(!recorder||finishingRef.current)return;finishingRef.current=true;recordingRef.current=null;try{await recorder.stopAndUnloadAsync();const uri=recorder.getURI();if(!transcribe||!uri)return;changeMode("working");setMessage("Understanding…");const response=await fetch(uri),blob=await response.blob(),dataUrl=await blobToDataUrl(blob),base64=dataUrl.split(",")[1]||"",mimeType=blob.type||(/\.webm$/i.test(uri)?"audio/webm":"audio/m4a"),result=await taurantoApi.transcribeAudio(base64,normalizeMime(mimeType));const heard=String(result.transcript||"").trim();setTranscript(heard);if(!heard)throw new Error("I couldn't hear a clear instruction. Try again.");await submit(heard)}catch(e){setMessage(e instanceof Error?e.message:"Could not process that command.");resume()}finally{finishingRef.current=false}}
async function submit(value=transcript){const text=value.trim();if(!text)return;changeMode("working");setMessage("Building a safe action plan…");try{await onCommand(draft(text));setTranscript("");setMessage("Command received. Tauranto created the action plan.");resume(650)}catch(e){setMessage(e instanceof Error?e.message:"Could not create the proposal.");resume(1200)}}
function resume(delay=0){setTimeout(()=>{if(enabledRef.current){changeMode("standby");void startWakeSession()}else changeMode("off")},delay)}async function speakNow(){if(mode==="recording"){await finishRecording(true);return}await startRecording()}const recording=mode==="recording",working=mode==="working";
if(!visible)return null;
return <View><View style={s.card}><View style={s.top}><View style={[s.dot,mode==="standby"&&s.ready,recording&&s.live]}/><View style={{flex:1}}><Text style={s.status}>{mode==="standby"?"Tauranto is listening":recording?"Listening to your command":working?"Working":"Tauranto is off"}</Text><Text style={s.statusCopy}>{mode==="standby"?"Say “Hey Tauranto”":recording?"Speak naturally — silence ends the command":working?"Understanding and validating your instruction":"Turn Tauranto on from here or Home"}</Text></View><Switch value={enabled} onValueChange={onEnabledChange} trackColor={{false:colors.line,true:colors.leaf}} thumbColor="white"/></View><Text style={s.prompt}>{recording?"I’m listening.":working?"One moment…":"What needs to happen?"}</Text><Text style={s.help}>Try: “Hey Tauranto, we're out of lobster tonight.” No finish phrase is required.</Text>{!!transcript&&<View style={s.transcript}><Text style={s.label}>HEARD</Text><Text style={s.transcriptText}>“{transcript}”</Text></View>}{!!message&&<View style={s.message}><Ionicons name="information-circle-outline" size={20} color={colors.leafDeep}/><Text style={s.messageText}>{message}</Text></View>}{mode==="standby"&&!!lastHeard&&<Text style={s.last}>Heard nearby: “{lastHeard}”</Text>}<Pressable disabled={working} onPress={()=>void speakNow()} style={[s.speak,recording&&s.stop,working&&{opacity:.6}]}><View style={s.speakIcon}><Ionicons name={recording?"stop":"mic"} size={28} color="white"/></View><View style={{flex:1}}><Text style={s.speakTitle}>{recording?"Finish now":"Speak now"}</Text><Text style={s.speakCopy}>{recording?"Usually you can simply stop speaking":"Tap for immediate listening"}</Text></View></Pressable><Pressable onPress={()=>setManual(v=>!v)} style={s.type}><Ionicons name="keypad-outline" size={23} color={colors.ink}/><Text style={s.typeText}>{manual?"Close typing":"Type instead"}</Text></Pressable></View>{manual&&<View style={s.inputBar}><TextInput value={transcript} onChangeText={setTranscript} multiline placeholder="Type a restaurant instruction" style={s.input}/><Pressable onPress={()=>void submit()} style={s.send}><Ionicons name="arrow-up" size={22} color="white"/></Pressable></View>}</View>}
function blobToDataUrl(blob:Blob){return new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=()=>reject(reader.error);reader.readAsDataURL(blob)})}function normalizeMime(value:string){if(value.includes("webm"))return"audio/webm";if(value.includes("wav"))return"audio/wav";if(value.includes("mpeg")||value.includes("mp3"))return"audio/mpeg";return"audio/m4a"}
const s=StyleSheet.create({card:{backgroundColor:"#E8F0DB",borderRadius:26,padding:20,borderWidth:1,borderColor:"#D3DFC4",...shadow},top:{flexDirection:"row",alignItems:"center",gap:12},dot:{width:13,height:13,borderRadius:7,backgroundColor:colors.muted},ready:{backgroundColor:"#3F8A50"},live:{backgroundColor:colors.tomato},status:{fontFamily:"DMSans_700Bold",fontSize:18,color:colors.ink},statusCopy:{fontFamily:"DMSans_400Regular",fontSize:14,lineHeight:19,color:colors.inkSoft,marginTop:3},prompt:{fontFamily:"DMSans_700Bold",fontSize:30,lineHeight:37,color:colors.ink,marginTop:24},help:{fontFamily:"DMSans_400Regular",fontSize:14,lineHeight:21,color:colors.inkSoft,marginTop:8},transcript:{backgroundColor:"#FFFFFFD9",borderRadius:radius.md,padding:15,marginTop:15},label:{fontFamily:"DMSans_700Bold",fontSize:11,letterSpacing:1,color:colors.leafDeep},transcriptText:{fontFamily:"DMSans_600SemiBold",fontSize:17,lineHeight:24,color:colors.ink,marginTop:7},message:{flexDirection:"row",gap:8,backgroundColor:"#FFFFFF8F",borderRadius:13,padding:12,marginTop:13},messageText:{flex:1,fontFamily:"DMSans_500Medium",fontSize:13,lineHeight:18,color:colors.inkSoft},last:{fontFamily:"DMSans_500Medium",fontSize:12,color:colors.muted,marginTop:9},speak:{minHeight:72,borderRadius:18,backgroundColor:colors.leafDeep,flexDirection:"row",alignItems:"center",gap:12,paddingHorizontal:15,marginTop:17},stop:{backgroundColor:colors.tomato},speakIcon:{width:46,height:46,borderRadius:14,backgroundColor:"#FFFFFF22",alignItems:"center",justifyContent:"center"},speakTitle:{fontFamily:"DMSans_700Bold",fontSize:17,color:"white"},speakCopy:{fontFamily:"DMSans_400Regular",fontSize:13,color:"#FFFFFFD0",marginTop:3},type:{height:56,borderRadius:16,backgroundColor:colors.paper,flexDirection:"row",alignItems:"center",justifyContent:"center",gap:9,borderWidth:1,borderColor:"#D4DDCA",marginTop:10},typeText:{fontFamily:"DMSans_700Bold",fontSize:15,color:colors.ink},inputBar:{flexDirection:"row",alignItems:"flex-end",backgroundColor:colors.paper,borderRadius:radius.md,borderWidth:1,borderColor:colors.line,marginTop:10,padding:9,gap:8},input:{flex:1,minHeight:50,maxHeight:110,fontFamily:"DMSans_400Regular",fontSize:16,color:colors.ink,padding:10},send:{width:50,height:50,borderRadius:15,backgroundColor:colors.leafDeep,alignItems:"center",justifyContent:"center"}});