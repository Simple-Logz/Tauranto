import type {VercelRequest,VercelResponse} from '@vercel/node';import {z} from 'zod';import {admin,fail,requireUser} from '../../server/http';
const create=z.object({restaurantId:z.string().uuid(),tableId:z.string().uuid().optional(),tableName:z.string().min(1).max(40).optional(),guestName:z.string().max(100).optional(),guestPhone:z.string().max(30).optional(),partySize:z.number().int().min(1).max(100).default(1),consentToMessages:z.boolean().default(false),orderNotes:z.string().max(4000).optional(),orderTotal:z.number().nonnegative().optional()});
async function member(db:any,restaurantId:string,userId:string){const {data}=await db.from('restaurant_members').select('role,can_approve').eq('restaurant_id',restaurantId).eq('user_id',userId).eq('active',true).maybeSingle();if(!data)throw new Error('FORBIDDEN');return data}
function periodStart(period:string){const d=new Date(),x=new Date(d);if(period==='today')x.setHours(0,0,0,0);else if(period==='week'){const day=(x.getDay()+6)%7;x.setDate(x.getDate()-day);x.setHours(0,0,0,0)}else if(period==='month'){x.setDate(1);x.setHours(0,0,0,0)}else if(period==='quarter'){x.setMonth(Math.floor(x.getMonth()/3)*3,1);x.setHours(0,0,0,0)}else if(period==='year'){x.setMonth(0,1);x.setHours(0,0,0,0)}else return null;return x.toISOString()}
async function purgeExpiredHistory(db:any,restaurantId:string){const cutoff=new Date(Date.now()-30*24*60*60*1000).toISOString();const {data:expired,error}=await db.from('guest_sessions').select('id').eq('restaurant_id',restaurantId).in('status',['closed','cancelled']).lt('closed_at',cutoff);if(error)throw error;const ids=(expired||[]).map((x:any)=>x.id);if(!ids.length)return;const {error:eventError}=await db.from('guest_session_events').delete().in('session_id',ids);if(eventError)throw eventError;const {error:sessionError}=await db.from('guest_sessions').delete().eq('restaurant_id',restaurantId).in('id',ids);if(sessionError)throw sessionError}
export default async function handler(req:VercelRequest,res:VercelResponse){try{const user=await requireUser(req),db=admin();if(req.method==='GET'){const restaurantId=z.string().uuid().parse(req.query.restaurantId),period=String(req.query.period||'today'),history=String(req.query.history||'false')==='true';await member(db,restaurantId,user.id);await purgeExpiredHistory(db,restaurantId);let periodQuery=db.from('guest_sessions').select('id,party_size,status,payment_status,opened_at,closed_at').eq('restaurant_id',restaurantId);const start=periodStart(period);if(start)periodQuery=periodQuery.gte('opened_at',start);const retentionCutoff=new Date(Date.now()-30*24*60*60*1000).toISOString();const [{data:active,error},{data:tables},{data:periodRows},{data:closed}]=await Promise.all([db.from('guest_sessions').select('*,dining_tables(name,section)').eq('restaurant_id',restaurantId).neq('status','closed').neq('status','cancelled').order('opened_at',{ascending:false}),db.from('dining_tables').select('*').eq('restaurant_id',restaurantId).eq('active',true).order('name'),periodQuery,history?db.from('guest_sessions').select('*,dining_tables(name,section)').eq('restaurant_id',restaurantId).in('status',['closed','cancelled']).gte('closed_at',retentionCutoff).order('closed_at',{ascending:false}).limit(250):Promise.resolve({data:[]})]);if(error)throw error;const rows=periodRows||[],sessions=active||[];return res.json({sessions,tables:tables||[],history:closed||[],historyRetentionDays:30,period,metrics:{guests:rows.reduce((n:number,x:any)=>n+(x.party_size||0),0),parties:rows.length,activeGuests:sessions.reduce((n:number,x:any)=>n+(x.party_size||0),0),openTables:sessions.length,closedTables:rows.filter((x:any)=>x.status==='closed').length,checks:rows.filter((x:any)=>['link_created','paid','completed'].includes(String(x.payment_status))).length}})}if(req.method==='POST'){const body=create.parse(req.body);await member(db,body.restaurantId,user.id);let tableId=body.tableId;if(!tableId&&body.tableName){const {data:t,error:e}=await db.from('dining_tables').upsert({restaurant_id:body.restaurantId,name:body.tableName},{onConflict:'restaurant_id,name'}).select().single();if(e)throw e;tableId=t.id}const {data:payment}=await db.from('restaurant_payment_settings').select('checkout_base_url,active').eq('restaurant_id',body.restaurantId).maybeSingle();const insert:any={restaurant_id:body.restaurantId,table_id:tableId,server_id:user.id,guest_name:body.guestName||null,guest_phone:body.guestPhone||null,party_size:body.partySize,consent_to_messages:body.consentToMessages,status:body.orderNotes?'ordering':'seated',order_notes:body.orderNotes||null,order_total:body.orderTotal??null,payment_status:body.orderTotal!=null&&payment?.active?'ready':'not_requested'};const {data,error}=await db.from('guest_sessions').insert(insert).select().single();if(error)throw error;let paymentUrl:string|null=null;if(body.orderTotal!=null&&payment?.active&&payment.checkout_base_url){const join=payment.checkout_base_url.includes('?')?'&':'?';paymentUrl=`${payment.checkout_base_url}${join}tauranto_session=${encodeURIComponent(data.id)}&amount=${encodeURIComponent(body.orderTotal.toFixed(2))}`;await db.from('guest_sessions').update({payment_url:paymentUrl,payment_status:'link_created'}).eq('id',data.id)}await db.from('guest_session_events').insert([{restaurant_id:body.restaurantId,session_id:data.id,actor_id:user.id,event_type:'session_opened',summary:`Party of ${body.partySize} seated`,payload:{tableId}},{restaurant_id:body.restaurantId,session_id:data.id,actor_id:user.id,event_type:'order_captured',summary:body.orderNotes?'Initial order captured':'No initial order',payload:{orderNotes:body.orderNotes||null,orderTotal:body.orderTotal??null}}]);return res.status(201).json({session:{...data,payment_url:paymentUrl}})}return res.status(405).json({error:'METHOD_NOT_ALLOWED'})}catch(e){return fail(res,e)}}