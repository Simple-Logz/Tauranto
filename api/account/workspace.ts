import type {VercelRequest,VercelResponse} from "@vercel/node";
import {admin,fail,requireUser} from "../../server/http";
export default async function handler(req:VercelRequest,res:VercelResponse){try{const user=await requireUser(req),restaurantId=String(req.method==="GET"?req.query.restaurantId:req.body?.restaurantId||"");if(!restaurantId)throw new Error("RESTAURANT_REQUIRED");const db=admin();const {data:member}=await db.from("restaurant_members").select("role,can_approve,active").eq("restaurant_id",restaurantId).eq("user_id",user.id).eq("active",true).single();if(!member)throw new Error("FORBIDDEN");if(req.method==="GET"){const [{data:profile},{data:restaurant},{data:members},{data:preferences}]=await Promise.all([db.from("profiles").select("id,full_name,email,phone,avatar_url").eq("id",user.id).single(),db.from("restaurants").select("id,name,timezone,approval_mode,phone,website,address,logo_url").eq("id",restaurantId).single(),db.from("restaurant_members").select("user_id,role,can_approve,active,profiles(full_name,email)").eq("restaurant_id",restaurantId).eq("active",true),db.from("restaurant_preferences").select("notifications,voice,privacy").eq("restaurant_id",restaurantId).maybeSingle()]);return res.json({profile,restaurant,members:members||[],preferences:preferences||{notifications:{approval_email:true,command_failures:true,weekly_summary:true},voice:{standby:true,spoken_confirmations:true,language:"en-US"},privacy:{retain_audio:false,activity_retention_days:90}}})}if(req.method==="PATCH"){if(!["owner","admin"].includes(member.role))throw new Error("FORBIDDEN");const {profile,restaurant,preferences}=req.body||{};if(profile)await db.from("profiles").update({full_name:profile.full_name,phone:profile.phone,avatar_url:profile.avatar_url}).eq("id",user.id);if(restaurant)await db.from("restaurants").update({name:restaurant.name,timezone:restaurant.timezone,approval_mode:restaurant.approval_mode,phone:restaurant.phone,website:restaurant.website,address:restaurant.address,logo_url:restaurant.logo_url}).eq("id",restaurantId);if(preferences)await db.from("restaurant_preferences").upsert({restaurant_id:restaurantId,...preferences,updated_at:new Date().toISOString()});return res.json({ok:true})}return res.status(405).json({error:"METHOD_NOT_ALLOWED"})}catch(error){return fail(res,error)}}
