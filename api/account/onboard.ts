import type {VercelRequest,VercelResponse} from '@vercel/node';import crypto from 'crypto';import {z} from 'zod';import {admin,fail,requireUser} from '../../server/http';
const bodySchema=z.object({restaurantName:z.string().min(2).max(120),dinerBand:z.enum(['0-100','100-300','300-500','500-1000+'])});
const norm=(s:string)=>s.trim().toLowerCase().replace(/\s+/g,' ');const code=()=>`TR-${crypto.randomBytes(4).toString('hex').toUpperCase()}`;const secret=()=>crypto.randomBytes(18).toString('base64url');const hash=(v:string)=>crypto.createHash('sha256').update(v).digest('hex');
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST')return res.status(405).json({error:'METHOD_NOT_ALLOWED'});try{const user=await requireUser(req),body=bodySchema.parse(req.body),db=admin();const {data:existingMembership}=await db.from('restaurant_members').select('restaurant_id').eq('user_id',user.id).eq('active',true).maybeSingle();if(existingMembership)return res.status(409).json({error:'ALREADY_HAS_RESTAURANT'});const normalizedName=norm(body.restaurantName);const {data:existing}=await db.from('restaurants').select('id,name,public_code').eq('normalized_name',normalizedName).maybeSingle();if(existing)return res.status(409).json({error:'RESTAURANT_ALREADY_REGISTERED',restaurantCodeHint:existing.public_code?String(existing.public_code).slice(0,4)+'…':undefined});const claimSecret=secret(),publicCode=code();const {data:restaurant,error}=await db.from('restaurants').insert({name:body.restaurantName.trim(),normalized_name:normalizedName,public_code:publicCode,diner_band:body.dinerBand,ownership_status:'pending',owner_claim_hash:hash(claimSecret)}).select('id,name,public_code,diner_band,ownership_status').single();if(error)throw error;await db.from('restaurant_members').insert({restaurant_id:restaurant.id,user_id:user.id,role:'owner',can_approve:true,active:true,can_view_all_tables:true,can_close_tables:true,can_restore_history:true,can_issue_checkout:true});return res.status(201).json({restaurant,ownerClaimCode:claimSecret,warning:'Save this ownership recovery code securely. It is shown only once.'})}catch(e){return fail(res,e)}}
