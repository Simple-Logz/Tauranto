import OpenAI from "openai";import {z} from "zod";import {ACTION_INTENTS} from "../src/lib/governance";
const actions=[...ACTION_INTENTS,"unknown"] as const;
export const proposalSchema=z.object({action_type:z.enum(actions),title:z.string().min(1),summary:z.string().min(1),parameters:z.record(z.string(),z.union([z.string(),z.number(),z.boolean(),z.null()])),risk:z.enum(["low","medium","high","critical"]),confidence:z.number().min(0).max(1),ambiguities:z.array(z.string()),confirmation_question:z.string().nullable()});
export type Proposal=z.infer<typeof proposalSchema>;
// A single utterance can contain more than one independent instruction
// across unrelated systems ("pause delivery, tell Sarah, move the meeting").
// interpretWithAI always returns an array so the caller can validate, plan,
// risk-score and approve each instruction on its own — most transcripts
// naturally decompose into exactly one action.
const multiProposalSchema=z.object({actions:z.array(proposalSchema).min(1).max(5)});
function actionModel(){const configured=(process.env.OPENAI_ACTION_MODEL||"").trim();return !configured||configured==="gpt-5.4"?"gpt-5.6":configured}
export async function interpretWithAI(transcript:string,restaurantContext:string):Promise<Proposal[]>{if(!process.env.OPENAI_API_KEY)throw new Error("OPENAI_API_KEY_NOT_CONFIGURED");const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const response=await client.responses.parse({model:actionModel(),instructions:`You are Tauranto, a high-reliability restaurant operations assistant. Convert speech into one or more executable operations. Most instructions are a single operation — return exactly one action in that case. Some instructions genuinely contain several independent operations across different systems or purposes spoken in one breath (for example: "pause delivery for 30 minutes, tell Sarah to come in, and move tomorrow's meeting to 10"); when that happens, return one action object per instruction, in the order spoken, each fully valid and independently actionable on its own. Never artificially split a single instruction into sub-steps, and never merge genuinely separate instructions into one action. Connected systems can include Google Calendar with Google Meet conferencing, Gmail, Zoom, HubSpot, Square, Toast, Slack, website/custom webhook and messaging. Never invent recipients, IDs, emails, phone numbers, business facts, prices or order details. Restaurant context contains connected systems and the real operations directory. Resolve contacts from that directory when possible; if a named person/company is not resolvable and an email/phone is required, ask ONE concise clarification for that specific action.
Restaurant context: ${restaurantContext}
Resolve relative dates/times using restaurant timezone and current timestamp. Treat an explicitly supplied timezone or common US timezone abbreviation such as EST, EDT, CST, CDT, MST, MDT, PST or PDT as sufficient timezone clarification; do not ask again whether it means local time. Preserve the user's intended wall-clock time and resolve it to the correct ISO-8601 offset. For schedule_meeting return subject/title, start_time as an ISO-8601 datetime with offset, duration_minutes (default 30 only when user gives no duration), timezone, attendee_name and attendee_email when known. For ordinary video meetings default video_provider='google_meet'; set video_provider='zoom' only when Zoom is explicitly requested. Google Meet is created through the connected Google Calendar integration and the attendee receives the calendar invitation. Use zoom_meeting only when creating a Zoom meeting without a calendar event. Use calendar_reminder for reminders/tasks rather than meetings. Use send_email for ordinary Gmail/email. Use crm_contact_create/crm_contact_update/crm_note/crm_task for HubSpot. Use square_catalog_update or square_order for Square. Use toast_menu_update or toast_order_action for Toast. Use website_update for connected website changes. Use menu_availability/business_hours/pause_orders when the user's request should propagate operationally to all applicable connected systems. Use contact_message for SMS/email to a directory contact when it is not better represented by send_email/supplier_email. Use supplier_email for vendor communications and purchase_request for a supply/order commitment. Preserve names, quantities, units, money and requested times exactly. If a critical field is missing for one action, add it to that action's ambiguities and ask ONE concise question for it — a missing field in one action never blocks the other independent actions in the same transcript. Do not reject an understandable command merely because an optional field is absent. Destructive or financial actions are high/critical risk; ordinary scheduling, reminders, CRM notes and messages are low/medium risk.`,input:transcript,text:{format:restaurantActionFormat()}});if(!response.output_parsed)throw new Error("AI_RETURNED_NO_ACTION");const parsed=multiProposalSchema.parse(response.output_parsed);return parsed.actions}
function restaurantActionFormat(){const action={type:"object",additionalProperties:false,required:["action_type","title","summary","parameters","risk","confidence","ambiguities","confirmation_question"],properties:{action_type:{type:"string",enum:[...actions]},title:{type:"string"},summary:{type:"string"},parameters:{type:"object",additionalProperties:{type:["string","number","boolean","null"]}},risk:{type:"string",enum:["low","medium","high","critical"]},confidence:{type:"number",minimum:0,maximum:1},ambiguities:{type:"array",items:{type:"string"}},confirmation_question:{type:["string","null"]}}};const json={type:"object",additionalProperties:false,required:["actions"],properties:{actions:{type:"array",minItems:1,maxItems:5,items:action}}};return{type:"json_schema" as const,name:"restaurant_actions",strict:false,schema:json}}
